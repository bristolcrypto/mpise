# TODO

1. clean-up:

   1. finalise source code, e.g., updates from Johann, fine-tuning of documentation, etc.
   2. finalise experimental results, i.e., Isaar to rerun after 1. is complete
   3. remove old/unused content, e.g., `${REPO_HOME}/old`, non-current entries in `${REPO_HOME}/data`, etc.

2. relocation:

   1. relocate repo. to `bristolcrypto` organisation
      - `https://github.com/scarv/mpise` to `https://github.com/bristolcrypto/mpise`
      - `https://github.com/isa084/cva6_crypto_base` to `https://github.com/bristolcrypto/mpise-cva6`

   2. update instructions to reflect relocation
   3. test end-to-end use of repo.

3. pre-print:

   1. make repo. public
   2. publish pre-print with repo. URLs included

4. artefact:

   1. refine scripting to allow semi-automatic generation of results in paper, or at least document how
