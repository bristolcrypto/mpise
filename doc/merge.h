#ifndef __MERGE_H
#define __MERGE_H

#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

#define SIZEOF(x) (     sizeof(x) )
#define BITSOF(x) ( 8 * sizeof(x) )

typedef uint8_t limb_t;
typedef uint16_t dlimb_t;

#endif
